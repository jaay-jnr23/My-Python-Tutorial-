"print 'Hello, world!'"
# this is a SyntaxError because there is no parentheses in call to the PRINT function. Parentheses() 
# is one of the major order of precedence in python that's always attach to every function in python. without this parentheses(), functions in python cannot be assigned.
# for us to get an output of this function we need to add a parentheses so that the python interpreter can carryout it work on this.e.g
"print('Hello, world!')"
# and we get "Hello, world" as our output.


'''1/2'''
# this is an ARITHEMETIC OPERATION that cannot bring out an OUTPUT because no function is being assign to it. it is not an error, just that there's no function attached to it to carry out a result.
# so for us to get an output we need to assign the print() function. e.g
'''print(1/2)'''
# In this way our python intepreter is able to interprete the operation and we get 0.5 as our output because this is a division operation


'''type(1/2)'''
# in this process we are asking our python interpreter what type of data is been typed in but, no output is being display because, we didn't assign a function to the operation
# so for us to get an output of this operation we assign the print() function to the operation just like this e.g
'''print(type(1/2))'''
# so therefore, we get our result as "FLOAT". FLOAT is a type of data in python whereby the output is always in decimal or fractions.


'''print(01)'''
# This is a SyntaxError because, zeros can't be use to start a number exspecially INTEGERS in python, except for decimal number i.e FLOAT and you can't assign a print() function to a number starting with zero
# so for us to get an output of this operation and for the  understanding of the python interpreter, we input this...
'''print(1)'''
# so our result is (1) an integer.



'''1/(2/3)'''
# NO OUTPUT here because, no function is assign to it
# so we assign the print() function here to get an output e.g
'''print(1/(2/3))'''
#  so we get 1.5 as our answer which is a FLOAT in python.


# Most of the questions here, Function are not assign to them...so for our python interpreter to be able to interprete operations in python, functions must be assign to the operation, there are different kinds of functions in python which are
#  print() function
# input() function
# reverse() function
# length() function or len() function.

